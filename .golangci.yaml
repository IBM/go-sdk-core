version: "2"
linters:
  enable:
    - gosec
  exclusions:
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
  settings:
    staticcheck:
      checks:
        - all
        - -ST1005
        - -ST1003
run:
  timeout: 2m
